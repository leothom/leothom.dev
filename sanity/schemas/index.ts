import project from './project-schema';
import page from './page-schema';
import stack from './stack-schema';
import timeline from './timeline-schema';

const schemas = [project, page, stack, timeline];

export default schemas;